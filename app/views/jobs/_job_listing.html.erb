<div class="container">
  <div class="row page-title text-center wow bounce"  data-wow-delay="1s">
    <h2>Jobs</h2>
    <%= will_paginate jobs %>
  </div>


<div class="row jobs">
  <div class="col-md-9">
    <div class="job-posts table-responsive">
      <table class="table">
        <% jobs.each_with_index do |job, index| %>
          <tr class="odd wow fadeInUp" data-wow-delay="<%(1+index).to_s%>s">
        <!--The above (1+index) is to stagger the appearance of the jobs in the table -->
          <td class="tbl-title"><h4><%= link_to job.title, job%> </h4></td>

          <td><p><%=job.user.name%> : <%=job.job_type%></p></td>

          <td><p><i class="glyphicon glyphicon-globe"></i> <%=job.city%>, <% unless job.state.empty? %> <%=job.state %> <% else %> <%= job.country %> <% end %> </p></td>

          <td><p><%=job.salary%></p></td>


          </tr>
        <% end %>
      </table>
    </div>

</div>
<div class="col-md-3 hidden-sm">
    <div class="job-add wow fadeInRight animated" data-wow-delay="1.5s" style="visibility: visible; animation-delay: 1.5s; animation-name: fadeInRight;">
      <h2>Want to Post a Job?</h2>
        <%= link_to "Post a Job", new_user_registration_path%>
    </div>
  </div>
</div>

<hr>