<div>
<%= form_for(@visitor) do |f| %>
  <% if @visitor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visitor.errors.count, "error") %> prohibited this visitor from being saved:</h2>

      <ul>
      <% @visitor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="form">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

  <div class="form">
    <%= f.submit "Subscribe", :class => "navbar-btn nav-button wow bounceInRight login" %>
  </div>
<% end %>
</div>
